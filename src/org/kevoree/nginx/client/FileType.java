package org.kevoree.nginx.client;

/**
 * Defines the representable file types
 *
 */
public enum FileType {
	DIR, AVI, DOC, HTML, IMG, JPEG, MOV, MP3, PDF, PNG, PPTX, PST, TXT, TIFF, WAV, WMA, WMV, WPD, XLSX, ZIP, MM, OTHER
};
